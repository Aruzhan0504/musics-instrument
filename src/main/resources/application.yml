spring:
  application:
    name: musics-instrument

  datasource:
    url: jdbc:postgresql://localhost:5432/bitlab
    username: postgres
    password: Aru767785
  liquibase:
    change-log: classpath:db.changelog/changelog.xml

  jpa:
    hibernate:
      ddl-auto: none
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 5MB
server:
  port: 8089